import test from 'japa'

test.group('Ficha', () => {
  test('Deverá adicionar uma ficha no banco de dados', (assert) => {
    assert
  })

  test('Deverá alterar uma ficha', (assert) => {
    assert
  })

  test('Deverá retornar uma ficha', (assert) => {
    assert
  })

  test('Deverá desativar uma ficha', (assert) => {
    assert
  })
})
