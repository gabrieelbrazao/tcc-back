import test from 'japa'

test.group('Ficha', () => {
  // manda todas as informacoes e retorna nada
  test('Deverá adicionar uma ficha no banco de dados', (assert) => {
    assert
  })

  // manda id e informacoes e retorna nada
  test('Deverá alterar uma ficha', (assert) => {
    assert
  })

  // retornar todos os dados e manda o id da ficha
  test('Deverá retornar uma ficha', (assert) => {
    assert
  })

  // nome do cliente, imagem do rosto, tratamentos, observacoes e orientações da ultima consulta, data, id
  test('Deverá retornar todas as fichas do usuario', (assert) => {
    assert
  })
})
