import test from 'japa'

test.group('Cliente', () => {
  test('Deverá adicionar um cliente no banco de dados', (assert) => {
    assert
  })

  test('Deverá alterar um cliente', (assert) => {
    assert
  })

  test('Deverá retornar um cliente', (assert) => {
    assert
  })

  test('Deverá desativar um cliente', (assert) => {
    assert
  })
})
